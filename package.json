{
	"name": "swanki-monorepo",
	"private": true,
	"packageManager": "bun@1.4.2",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev": "turbo dev",
		"dev:web": "turbo dev --filter=web",
		"dev:docs": "turbo dev --filter=docs",
		"dev:mobile": "cd apps/web && BETTER_AUTH_SECRET=mobile-dev-dummy BETTER_AUTH_URL=http://localhost:3000 vite dev --config vite.config.mobile.ts",
		"build:mobile": "cd apps/web && mkdir -p data && BETTER_AUTH_SECRET=mobile-build-dummy BETTER_AUTH_URL=http://localhost:3000 vite build --config vite.config.mobile.ts",
		"start:desktop": "cd apps/desktop && bun x electron-forge start",
		"dev:desktop": "turbo dev --filter=desktop",
		"build": "turbo build",
		"build:desktop": "turbo build --filter=desktop",
		"package:desktop": "cd apps/desktop && bun x electron-forge package",
		"make:desktop": "cd apps/desktop && bun x electron-forge make",
		"lint": "turbo lint",
		"lint:fix": "turbo lint:fix",
		"test": "turbo test",
		"test:ci": "turbo test:ci",
		"db:generate": "cd apps/web && bun x drizzle-kit generate",
		"db:push": "cd apps/web && bun x drizzle-kit push",
		"prepare": "lefthook install"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.12",
		"@commitlint/cli": "^21.2.2",
		"@commitlint/config-conventional": "^21.2.2",
		"@commitlint/types": "^21.2.0",
		"bun-types": "^1.4.2",
		"lefthook": "^2.1.12",
		"turbo": "^2.10.12"
	}
}
